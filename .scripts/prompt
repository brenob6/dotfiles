# !/bin/bash
pwr="Power Off "
rst="Restart "
lck="Logout "
chosen=$(printf "$pwr\n$rst\n$lck" | rofi -theme "/home/breno/.config/rofi/select" -dmenu -i)

case "$chosen" in
	"$pwr") poweroff ;;
	"$rst") reboot ;;
	"$lck") i3 exit ;;
	*) exit 1 ;;
esac

